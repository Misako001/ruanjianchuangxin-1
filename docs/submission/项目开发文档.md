# VisionGenie项目开发文档

## 1 项目概述

### 1.1 项目背景

随着智能手机和社交媒体的普及，照片拍摄和分享已经成为人们日常生活的重要组成部分。然而，专业级的照片调色需要专业知识和复杂的操作，对于普通用户来说门槛较高。同时，传统的调色工具操作复杂，学习成本高，难以满足用户快速、便捷地获得专业调色效果的需求。

在AI技术快速发展的背景下，语音交互和智能图像处理技术为照片调色带来了新的可能性。VisionGenie项目旨在利用AI技术和语音控制，降低照片调色的门槛，让普通用户也能轻松实现专业级的调色效果。

### 1.2 项目定位

#### 1.2.1 应用场景

- **个人日常拍照**：用户拍摄的日常照片需要快速调色美化
- **社交媒体分享**：用户需要为社交媒体准备高质量的照片
- **摄影爱好者**：摄影爱好者需要专业的调色工具，但希望操作更便捷
- **内容创作者**：内容创作者需要高效地处理大量照片

#### 1.2.2 目标人群

- **普通用户**：希望快速美化照片，不具备专业调色知识
- **摄影爱好者**：具备一定摄影知识，追求更高质量的调色效果
- **内容创作者**：需要高效处理大量照片的博主、网红等
- **专业摄影师**：需要快速预览和调整照片的专业人士

### 1.3 项目方案

VisionGenie采用React Native跨平台开发框架，结合云端AI服务和本地GPU渲染技术，实现智能照片调色功能。项目主要包含以下核心模块：

- **智能语音调色**：通过语音指令控制调色参数，支持连续语音交互
- **AI自动调色**：基于图像分析的首轮视觉建议，智能推荐调色方案
- **实时GPU渲染**：60fps实时预览，支持23个专业调色参数
- **2D转3D**：将2D照片转换为3D模型
- **社区分享**：发布调色作品，浏览社区内容，互动交流

### 1.4 项目目标

- 开发一款跨平台的智能照片调色应用，支持Android和iOS
- 实现语音控制调色功能，提供自然的交互体验
- 集成AI自动调色功能，为用户提供智能推荐
- 实现实时GPU渲染，确保调色效果的实时预览
- 集成2D转3D功能，为用户提供更多创意可能性
- 建立社区分享平台，促进用户间的互动和交流
- 确保应用的稳定性和性能，提供流畅的用户体验

### 1.5 项目价值

- **降低调色门槛**：通过语音控制和AI推荐，让普通用户也能轻松实现专业级调色效果
- **提高效率**：语音控制和实时预览大大提高了调色的效率
- **促进创意表达**：丰富的调色参数和2D转3D功能为用户提供更多创意可能性
- **建立社区生态**：社区分享功能促进用户间的交流和学习
- **技术创新**：结合AI、语音识别和GPU渲染技术，推动移动图像处理技术的发展

## 2 开发计划

### 2.1 最终呈现形式

- **移动应用**：Android和iOS平台的移动应用
- **Web端**：PC端社区功能，支持发布、评论帖子
- **后端服务**：提供AI解析、3D建模和社区功能的后端服务
- **API接口**：为前端应用提供数据和功能支持的RESTful API

### 2.2 主要功能描述

#### 智能调色系统

- **语音调色**：使用Android原生语音识别，支持中文语音输入，连续交互，实时反馈，支持撤销功能
- **AI自动调色**：上传图片后自动分析并生成初始调色方案，智能识别人像、风景、天空等场景，基于图像统计信息自动调整23个调色参数
- **GPU实时渲染**：双引擎架构（Legacy模式和Pro模式），60fps实时预览，支持曝光、对比度、色温、曲线、色轮等23个参数

#### 2D转3D

- **Tripo集成**：使用Tripo API进行2D到3D转换
- **多格式输出**：支持GLB等多种3D格式
- **纹理支持**：可选PBR材质和纹理
- **任务管理**：异步任务处理，支持进度查询

#### AI助手

- **智能规划**：基于用户需求生成操作计划
- **执行引擎**：自动执行复杂的调色任务
- **记忆系统**：保存用户偏好和历史记录
- **自然交互**：支持自然语言对话

#### 社区功能

- **内容发布**：发布调色作品，支持前后对比
- **内容浏览**：按标签筛选，瀑布流展示
- **互动功能**：点赞、收藏、评论
- **草稿管理**：保存未完成的作品

### 2.3 运行环境

#### 前端

- **操作系统**：Android 8.0+，iOS 13.0+
- **设备要求**：支持GPU渲染的移动设备
- **网络要求**：需要网络连接以使用云端AI服务
- **开发状态**：目前仅开发了Android端，iOS版本为开发中

#### 后端

- **操作系统**：Linux/Windows/macOS
- **Node.js**：v22.11.0+
- **数据库**：MySQL
- **网络要求**：需要网络连接以访问外部AI服务

### 2.4 验收标准

- **功能完整性**：所有核心功能均已实现并正常工作
- **性能要求**：GPU渲染达到60fps，语音识别响应时间小于1秒
- **稳定性**：应用运行稳定，无崩溃或严重错误
- **用户体验**：界面美观，操作流畅，交互自然
- **兼容性**：在主流Android和iOS设备上正常运行

### 2.5 关键问题

- **语音识别准确性**：确保语音指令能够准确识别和解析
- **AI模型性能**：确保AI模型能够快速、准确地分析图像和生成调色方案
- **GPU渲染性能**：确保在不同设备上都能实现流畅的实时预览
- **网络依赖**：实现离线模式，确保在网络不可用时仍能提供基本功能
- **数据隐私**：确保用户数据的安全和隐私保护

### 2.6 进度安排

- **开发模式**：双线开发（根据团队分工）
- **协作方式**：通过GitHub仓库进行代码同步和版本控制
- **沟通机制**：每天对齐开发颗粒度，确保进度和质量
- **开发阶段**：
  1. **需求分析与设计**：完成需求分析、系统设计和UI设计
  2. **核心功能开发**：并行开发智能调色、AI Agent、2D转3D和社区功能
  3. **集成测试**：进行功能测试、性能测试和兼容性测试
  4. **优化与部署**：优化性能，部署后端服务和前端应用

## 3 可行性分析

### 3.1 技术可行性分析

- **前端技术**：React Native是成熟的跨平台开发框架，能够满足移动应用的开发需求
- **后端技术**：Node.js和Express是成熟的后端开发技术，能够支持API服务和AI集成
- **AI技术**：SiliconFlow API和Tripo API提供了成熟的AI和3D建模服务
- **GPU渲染**：@shopify/react-native-skia提供了强大的GPU渲染能力
- **语音识别**：Android原生语音识别模块能够满足语音控制的需求

### 3.2 资源可行性分析

- **开发人员**：需要前端、后端和AI开发人员
- **硬件资源**：需要开发设备和测试设备
- **服务器资源**：需要云服务器来部署后端服务
- **API资源**：需要购买SiliconFlow和Tripo等API服务

### 3.3 市场可行性分析

- **市场需求**：随着社交媒体的普及，照片调色需求持续增长
- **竞争分析**：市场上已有一些调色应用，但大多缺乏AI和语音控制功能
- **用户接受度**：AI和语音控制是当前技术热点，用户接受度高
- **商业模式**：可以通过应用内购买、订阅和广告等方式实现盈利

## 4 需求分析

### 4.1 数据需求

#### 4.1.1 静态数据

- **调色预设**：内置的调色风格和参数配置
- **UI资源**：应用界面的图片、图标等资源
- **系统配置**：应用的默认配置和设置项

#### 4.1.2 动态数据

- **用户数据**：用户账号、个人信息和设置
- **调色历史**：用户的调色历史记录
- **社区内容**：用户发布的作品、评论和互动数据
- **3D模型数据**：2D转3D生成的模型数据

#### 4.1.3 数据词典

| 数据项 | 类型 | 描述 |
|--------|------|------|
| user_id | string | 用户唯一标识 |
| username | string | 用户名 |
| email | string | 用户邮箱 |
| password | string | 密码（加密存储） |
| photo_id | string | 照片唯一标识 |
| photo_url | string | 照片存储路径 |
| color_params | object | 调色参数配置 |
| post_id | string | 社区帖子唯一标识 |
| post_content | object | 帖子内容（包含前后对比照片） |
| comment_id | string | 评论唯一标识 |
| comment_content | string | 评论内容 |
| like_count | number | 点赞数 |
| save_count | number | 收藏数 |
| task_id | string | 3D建模任务唯一标识 |
| task_status | string | 任务状态（pending, processing, completed, failed） |
| model_url | string | 3D模型存储路径 |

#### 4.1.4 数据采集

- **用户输入**：用户上传的照片和语音指令
- **系统生成**：AI分析结果和3D模型数据
- **用户行为**：用户的调色操作和社区互动

### 4.2 功能需求

#### 4.2.1 智能调色模块

- **语音控制**：通过语音指令调整调色参数
- **AI自动调色**：基于图像分析自动生成调色方案
- **实时预览**：实时显示调色效果
- **参数调整**：手动调整23个调色参数
- **预设应用**：应用内置的调色预设

#### 4.2.2 2D转3D模块

- **照片上传**：上传2D照片
- **模型生成**：将2D照片转换为3D模型
- **模型预览**：预览生成的3D模型
- **模型导出**：导出3D模型文件

#### 4.2.3 AI助手模块

- **自然语言交互**：与用户进行自然语言对话
- **任务规划**：基于用户需求生成操作计划
- **自动执行**：自动执行复杂的调色任务
- **记忆管理**：保存用户偏好和历史记录

#### 4.2.4 社区模块

- **内容发布**：发布调色作品
- **内容浏览**：浏览社区作品
- **互动功能**：点赞、收藏、评论
- **用户关注**：关注其他用户
- **多端支持**：同时开发了Web端社区功能，用户可以在手机App浏览其他用户的帖子，也能在PC端发布、评论帖子

### 4.3 性能需求

#### 4.3.1 时间特性

- **语音识别响应时间**：小于1秒
- **AI分析响应时间**：小于3秒
- **GPU渲染帧率**：60fps
- **页面加载时间**：小于2秒

#### 4.3.2 适应性

- **设备适配**：支持不同屏幕尺寸和分辨率的设备
- **网络适配**：在不同网络环境下都能正常工作
- **系统适配**：支持不同版本的Android和iOS系统

### 4.4 界面需求

- **设计风格**：现代、简洁、专业
- **色彩方案**：以深色为主，突出照片内容
- **布局**：清晰的功能分区，易于操作
- **交互**：流畅的动画效果，自然的手势操作
- **响应式**：适配不同屏幕尺寸

### 4.5 接口需求

#### 4.5.1 硬件接口

- **摄像头**：访问设备摄像头拍摄照片
- **麦克风**：访问设备麦克风进行语音识别
- **GPU**：利用设备GPU进行实时渲染

#### 4.5.2 软件接口

- **SiliconFlow API**：用于AI图像分析和语音解析
- **Tripo API**：用于2D转3D功能
- **本地存储**：存储用户数据和照片
- **网络接口**：与后端服务通信

### 4.6 其他需求

- **安全性**：保护用户数据和隐私
- **可扩展性**：支持未来功能的扩展
- **可维护性**：代码结构清晰，易于维护
- **国际化**：支持多语言

## 5 概要设计

### 5.1 处理流程

#### 智能调色流程

1. 用户上传照片或从相册选择照片
2. 系统自动分析照片，生成首轮视觉建议
3. 用户可以选择应用AI建议或手动调整参数
4. 用户可以通过语音指令控制调色参数
5. 系统实时渲染调色效果
6. 用户可以保存调色结果或发布到社区

#### 2D转3D流程

1. 用户上传2D照片
2. 系统将照片发送到Tripo API进行处理
3. 系统查询任务状态
4. 任务完成后，系统获取3D模型
5. 用户可以预览和导出3D模型

#### 社区流程

1. 用户浏览社区内容
2. 用户可以点赞、评论和收藏作品
3. 用户可以发布自己的调色作品
4. 用户可以关注其他用户

### 5.2 总体结构设计

#### 前端结构

- **应用入口**：App.tsx
- **页面组件**：
  - GPUColorGradingScreen.tsx（智能调色主界面）
  - HomeHubScreen.tsx（首页）
  - AIAgentScreen.tsx（AI助手）
  - CommunityScreen.tsx（社区）
- **组件**：
  - GPUColorGradingView.tsx（GPU渲染组件）
  - 各种UI组件
- **模块**：
  - voice/（语音调色模块）
  - colorEngine/（调色引擎）
  - modules/（业务模块）

#### 后端结构

- **服务器入口**：server.js
- **模块**：
  - colorModule.js（调色模块）
  - modelingModule.js（3D建模模块）
  - agentModule.js（AI助手模块）
  - communityModule.js（社区模块）
- **服务**：
  - interpretService.js（语音解析）
  - autoGradeService.js（自动调色）
  - segmentationService.js（图像分割）
- **提供商**：
  - openaiCompat.js（OpenAI兼容接口）
  - fallback.js（降级策略）

### 5.3 功能设计

#### 智能调色模块

- **语音控制**：使用Android原生语音识别，支持连续语音交互
- **AI自动调色**：基于图像分析的首轮视觉建议
- **GPU实时渲染**：双引擎架构，60fps实时预览
- **参数调整**：支持23个专业调色参数

#### 2D转3D模块

- **Tripo集成**：使用Tripo API进行2D到3D转换
- **多格式输出**：支持GLB等多种3D格式
- **纹理支持**：可选PBR材质和纹理
- **任务管理**：异步任务处理

#### AI助手模块

- **智能规划**：基于用户需求生成操作计划
- **执行引擎**：自动执行复杂的调色任务
- **记忆系统**：保存用户偏好和历史记录
- **自然交互**：支持自然语言对话

#### 社区模块

- **内容发布**：发布调色作品，支持前后对比
- **内容浏览**：按标签筛选，瀑布流展示
- **互动功能**：点赞、收藏、评论
- **草稿管理**：保存未完成的作品

### 5.4 数据流转设计

#### 智能调色数据流

```
用户上传照片 → 前端处理 → 后端AI分析 → 生成调色方案 → 前端应用调色 → GPU渲染 → 实时预览
用户语音指令 → 语音识别 → 文本转录 → 后端AI解析 → 生成参数动作 → 前端应用参数 → GPU渲染 → 实时预览
```

#### 2D转3D数据流

```
用户上传照片 → 前端处理 → 后端发送到Tripo API → 任务处理 → 前端查询状态 → 获取3D模型 → 前端预览
```

#### 社区数据流

```
用户发布作品 → 前端处理 → 后端存储 → 社区展示
用户互动（点赞、评论） → 前端处理 → 后端更新 → 社区展示
```

### 5.5 用户界面设计

#### 主界面

- **顶部导航栏**：应用标题、设置按钮
- **中部预览区**：显示当前照片和调色效果
- **底部工具栏**：调色参数调整、语音控制按钮

#### 语音控制界面

- **语音识别状态**：显示语音识别结果
- **调色效果预览**：实时显示调色效果
- **撤销按钮**：撤销上一次操作

#### 社区界面

- **顶部导航栏**：社区标题、发布按钮
- **中部内容区**：瀑布流展示社区作品
- **底部导航栏**：首页、社区、我的

### 5.6 数据结构设计

#### 用户数据结构

```javascript
{
  user_id: "string",
  username: "string",
  email: "string",
  password: "string", // 加密存储
  avatar: "string", // 头像URL
  settings: {
    theme: "string",
    language: "string",
    notifications: boolean
  },
  created_at: "timestamp"
}
```

#### 照片数据结构

```javascript
{
  photo_id: "string",
  user_id: "string",
  original_url: "string",
  edited_url: "string",
  color_params: {
    exposure: number,
    contrast: number,
    temperature: number,
    // 其他调色参数
  },
  created_at: "timestamp"
}
```

#### 社区帖子数据结构

```javascript
{
  post_id: "string",
  user_id: "string",
  title: "string",
  before_url: "string",
  after_url: "string",
  description: "string",
  tags: ["string"],
  like_count: number,
  save_count: number,
  comment_count: number,
  created_at: "timestamp"
}
```

#### 3D建模任务数据结构

```javascript
{
  task_id: "string",
  user_id: "string",
  photo_url: "string",
  model_url: "string",
  status: "string", // pending, processing, completed, failed
  progress: number, // 0-100
  created_at: "timestamp",
  completed_at: "timestamp"
}
```

### 5.7 接口设计

#### 5.7.1 外部接口

- **SiliconFlow API**：用于AI图像分析和语音解析
- **Tripo API**：用于2D转3D功能
- **Google Cloud Storage**：用于存储用户照片和3D模型

#### 5.7.2 内部接口

##### 调色模块接口

| HTTP方法 | 路径 | 功能描述 |
|---------|------|----------|
| POST | /v1/modules/color/initial-suggest | 首轮视觉建议 |
| POST | /v1/modules/color/voice-refine | 语音调色解析 |
| POST | /v1/modules/color/pro/auto-grade | 专业自动调色 |
| POST | /v1/modules/color/pro/segment | 图像分割 |
| GET | /v1/modules/color/health | 健康检查 |

##### 3D建模模块接口

| HTTP方法 | 路径 | 功能描述 |
|---------|------|----------|
| POST | /v1/modules/modeling/jobs | 创建建模任务 |
| GET | /v1/modules/modeling/jobs/:taskId | 查询任务状态 |
| GET | /v1/modules/modeling/jobs/:taskId/assets | 获取任务资源 |
| POST | /v1/modules/modeling/capture-sessions | 创建捕获会话 |
| GET | /v1/modules/modeling/capture-sessions/:id | 查询捕获会话 |
| POST | /v1/modules/modeling/capture-sessions/:id/generate | 生成3D模型 |
| GET | /v1/modules/modeling/health | 健康检查 |

##### AI助手模块接口

| HTTP方法 | 路径 | 功能描述 |
|---------|------|----------|
| POST | /v1/modules/agent/plan | 生成操作计划 |
| POST | /v1/modules/agent/execute | 执行操作 |
| POST | /v1/modules/agent/memory/upsert | 更新记忆 |
| POST | /v1/modules/agent/memory/query | 查询记忆 |
| GET | /v1/modules/agent/health | 健康检查 |

##### 社区模块接口

| HTTP方法 | 路径 | 功能描述 |
|---------|------|----------|
| GET | /v1/modules/community/feed | 获取社区内容 |
| GET | /v1/modules/community/me/posts | 获取我的帖子 |
| POST | /v1/modules/community/drafts | 创建草稿 |
| PUT | /v1/modules/community/drafts/:id | 更新草稿 |
| POST | /v1/modules/community/drafts/:id/publish | 发布草稿 |
| POST | /v1/modules/community/posts/:id/like | 点赞 |
| POST | /v1/modules/community/posts/:id/save | 收藏 |
| GET | /v1/modules/community/posts/:id/comments | 获取评论 |
| POST | /v1/modules/community/posts/:id/comments | 发表评论 |
| GET | /v1/modules/community/health | 健康检查 |

##### 账户认证接口

| HTTP方法 | 路径 | 功能描述 |
|---------|------|----------|
| POST | /v1/auth/register | 用户注册 |
| POST | /v1/auth/login | 用户登录 |
| GET | /v1/profile/me | 获取个人信息 |
| PATCH | /v1/profile/me | 更新个人信息 |
| PATCH | /v1/profile/me/settings | 更新设置 |

### 5.8 错误/异常处理设计

#### 5.8.1 错误/异常输出信息

- **前端错误**：通过Toast或弹窗显示错误信息
- **后端错误**：返回标准化的错误响应，包含错误码和错误信息
- **日志记录**：记录详细的错误日志，便于排查问题

#### 5.8.2 错误/异常处理对策

- **网络错误**：显示网络连接失败提示，提供重试选项
- **API错误**：根据错误码显示相应的错误信息
- **AI服务错误**：自动降级到本地风格匹配
- **存储错误**：提示用户存储空间不足，建议清理空间
- **权限错误**：提示用户授予相应权限

### 5.9 系统配置策略

- **环境配置**：通过.env文件配置环境变量
- **API密钥**：安全存储API密钥，避免硬编码
- **默认设置**：提供合理的默认配置，减少用户配置负担
- **动态配置**：支持远程配置更新，无需重新发布应用

### 5.10 系统部署方案

#### 前端部署

- **Android**：通过Google Play Store发布
- **iOS**：通过Apple App Store发布
- **Web端**：
  - 开发环境启动：
    1. 进入Web端项目目录：`cd E:\GitHub_VisionGenie\ruanjianchuangxin-1\web`
    2. 运行启动命令：`npm run dev`
    3. 在浏览器中访问：`http://127.0.0.1:4020`
  - 生产环境部署：通过静态网站托管服务部署
- **测试版本**：通过TestFlight和内部测试渠道发布

#### 后端部署

- **服务器**：使用云服务器部署后端服务
- **数据库**：使用MySQL数据库
- **缓存**：使用Redis缓存热点数据
- **监控**：使用监控工具监控服务状态

### 5.11 跨端应用架构设计

- **框架**：使用React Native实现跨平台开发
- **原生模块**：使用原生模块实现平台特定功能（如语音识别）
- **代码共享**：共享业务逻辑代码，平台特定代码分离
- **构建流程**：使用CI/CD工具自动化构建和部署

### 5.12 其他相关技术与方案

- **性能优化**：使用Flash List优化长列表渲染，使用Fast Image组件优化图像加载
- **安全措施**：使用HTTPS加密传输，使用JWT进行身份验证
- **离线功能**：实现基本的离线调色功能，确保在网络不可用时仍能使用
- **国际化**：使用i18n实现多语言支持

## 6 数据库设计

### 6.1 数据库表结构

#### users表

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| user_id | VARCHAR(36) | PRIMARY KEY | 用户唯一标识 |
| username | VARCHAR(50) | NOT NULL | 用户名 |
| email | VARCHAR(100) | UNIQUE NOT NULL | 邮箱 |
| password_hash | VARCHAR(255) | NOT NULL | 密码哈希 |
| avatar | VARCHAR(255) | | 头像URL |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### photos表

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| photo_id | VARCHAR(36) | PRIMARY KEY | 照片唯一标识 |
| user_id | VARCHAR(36) | FOREIGN KEY | 用户ID |
| original_url | VARCHAR(255) | NOT NULL | 原始照片URL |
| edited_url | VARCHAR(255) | | 编辑后照片URL |
| color_params | JSON | | 调色参数 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### posts表

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| post_id | VARCHAR(36) | PRIMARY KEY | 帖子唯一标识 |
| user_id | VARCHAR(36) | FOREIGN KEY | 用户ID |
| title | VARCHAR(100) | NOT NULL | 标题 |
| before_url | VARCHAR(255) | NOT NULL | 编辑前照片URL |
| after_url | VARCHAR(255) | NOT NULL | 编辑后照片URL |
| description | TEXT | | 描述 |
| like_count | INTEGER | DEFAULT 0 | 点赞数 |
| save_count | INTEGER | DEFAULT 0 | 收藏数 |
| comment_count | INTEGER | DEFAULT 0 | 评论数 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### comments表

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| comment_id | VARCHAR(36) | PRIMARY KEY | 评论唯一标识 |
| post_id | VARCHAR(36) | FOREIGN KEY | 帖子ID |
| user_id | VARCHAR(36) | FOREIGN KEY | 用户ID |
| content | TEXT | NOT NULL | 评论内容 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### likes表

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| like_id | VARCHAR(36) | PRIMARY KEY | 点赞唯一标识 |
| post_id | VARCHAR(36) | FOREIGN KEY | 帖子ID |
| user_id | VARCHAR(36) | FOREIGN KEY | 用户ID |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### saves表

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| save_id | VARCHAR(36) | PRIMARY KEY | 收藏唯一标识 |
| post_id | VARCHAR(36) | FOREIGN KEY | 帖子ID |
| user_id | VARCHAR(36) | FOREIGN KEY | 用户ID |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### modeling_tasks表

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| task_id | VARCHAR(36) | PRIMARY KEY | 任务唯一标识 |
| user_id | VARCHAR(36) | FOREIGN KEY | 用户ID |
| photo_url | VARCHAR(255) | NOT NULL | 照片URL |
| model_url | VARCHAR(255) | | 模型URL |
| status | VARCHAR(20) | NOT NULL | 任务状态 |
| progress | INTEGER | DEFAULT 0 | 进度 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| completed_at | TIMESTAMP | | 完成时间 |

### 6.2 数据库索引

- **users表**：email字段创建唯一索引
- **posts表**：user_id字段创建索引，created_at字段创建索引
- **comments表**：post_id字段创建索引，user_id字段创建索引
- **likes表**：post_id字段创建索引，user_id字段创建索引
- **saves表**：post_id字段创建索引，user_id字段创建索引
- **modeling_tasks表**：user_id字段创建索引，status字段创建索引

### 6.3 数据库关系

- **users** 与 **photos**：一对多关系
- **users** 与 **posts**：一对多关系
- **users** 与 **comments**：一对多关系
- **users** 与 **likes**：一对多关系
- **users** 与 **saves**：一对多关系
- **users** 与 **modeling_tasks**：一对多关系
- **posts** 与 **comments**：一对多关系
- **posts** 与 **likes**：一对多关系
- **posts** 与 **saves**：一对多关系

## 7 手机端侧部署设计

### 7.1 手机环境需求

#### Android环境需求

- **操作系统**：Android 8.0+
- **RAM**：至少4GB
- **存储空间**：至少100MB
- **GPU**：支持OpenGL ES 3.0+
- **权限**：相机、麦克风、存储



### 7.2 部署流程

#### Android部署

1. 生成签名密钥
2. 配置build.gradle文件
3. 执行gradle assembleRelease命令
4. 生成APK文件
5. 通过Google Play Store发布



### 7.3 性能优化

- **应用体积**：优化资源文件，减少应用体积
- **启动时间**：使用懒加载，减少启动时间
- **运行内存**：优化内存使用，避免内存泄漏
- **电池消耗**：优化后台任务，减少电池消耗

## 8 详细设计

### 8.1 智能调色模块

#### 8.1.1 功能描述

智能调色模块是应用的核心功能，包括语音调色、AI自动调色和GPU实时渲染三个主要部分。该模块允许用户通过语音指令或AI推荐来调整照片的调色参数，并实时预览效果。

#### 8.1.2 性能描述

- **语音识别响应时间**：小于1秒
- **AI分析响应时间**：小于3秒
- **GPU渲染帧率**：60fps
- **参数调整响应时间**：小于100ms

#### 8.1.3 输入

- **用户上传的照片**：JPEG、PNG等格式
- **语音指令**：用户通过麦克风输入的语音
- **手动参数调整**：用户通过界面调整的调色参数

#### 8.1.4 输出

- **调色后的照片**：JPEG、PNG等格式
- **实时预览效果**：GPU渲染的实时预览
- **AI推荐的调色方案**：基于图像分析的调色参数

#### 8.1.5 程序逻辑

1. **照片上传**：用户选择照片后，系统加载照片并显示在预览区
2. **AI分析**：系统自动分析照片，生成首轮视觉建议
3. **语音控制**：用户按住语音按钮说话，系统识别语音并解析为调色指令
4. **参数应用**：系统将语音指令或手动调整的参数应用到照片上
5. **实时渲染**：GPU实时渲染调色效果，显示在预览区
6. **结果保存**：用户可以保存调色结果或发布到社区

#### 8.1.6 限制条件

- **照片大小**：最大支持10MB
- **语音识别**：仅支持中文
- **AI分析**：需要网络连接
- **GPU渲染**：依赖设备GPU性能

### 8.2 2D转3D模块

#### 8.2.1 功能描述

2D转3D模块允许用户将2D照片转换为3D模型，支持多种3D格式输出和纹理选项。

#### 8.2.2 性能描述

- **模型生成时间**：根据照片复杂度，通常为1-5分钟
- **模型预览帧率**：30fps
- **导出速度**：根据模型大小，通常为10-30秒

#### 8.2.3 输入

- **用户上传的2D照片**：JPEG、PNG等格式
- **模型参数**：用户选择的模型类型、纹理选项等

#### 8.2.4 输出

- **3D模型文件**：GLB等格式
- **模型预览**：实时3D模型预览

#### 8.2.5 程序逻辑

1. **照片上传**：用户选择照片后，系统加载照片
2. **任务创建**：系统向后端发送请求，创建3D建模任务
3. **任务处理**：后端将照片发送到Tripo API进行处理
4. **状态查询**：前端定期查询任务状态和进度
5. **模型获取**：任务完成后，前端获取3D模型
6. **模型预览**：前端显示3D模型预览
7. **模型导出**：用户可以导出3D模型文件

#### 8.2.6 限制条件

- **照片要求**：清晰、光照均匀、物体边界明确
- **网络连接**：需要网络连接
- **处理时间**：根据照片复杂度，可能需要较长时间
- **模型大小**：根据照片内容，模型大小可能较大

### 8.3 AI助手模块

#### 8.3.1 功能描述

AI助手模块允许用户通过自然语言与系统交互，系统根据用户需求生成操作计划并自动执行。

#### 8.3.2 性能描述

- **自然语言处理响应时间**：小于2秒
- **计划生成时间**：小于3秒
- **执行操作时间**：根据操作复杂度，通常为1-5秒

#### 8.3.3 输入

- **用户自然语言输入**：文本或语音
- **用户历史记录**：用户的调色历史和偏好

#### 8.3.4 输出

- **操作计划**：系统生成的调色操作计划
- **执行结果**：自动执行操作后的结果
- **自然语言回复**：系统对用户输入的回复

#### 8.3.5 程序逻辑

1. **用户输入**：用户通过文本或语音输入需求
2. **意图识别**：系统识别用户意图
3. **计划生成**：系统生成操作计划
4. **计划执行**：系统自动执行操作计划
5. **结果反馈**：系统向用户反馈执行结果
6. **记忆更新**：系统更新用户记忆，保存偏好

#### 8.3.6 限制条件

- **语言支持**：仅支持中文
- **网络连接**：需要网络连接
- **操作复杂度**：复杂操作可能需要多次交互

### 8.4 社区模块

#### 8.4.1 功能描述

社区模块允许用户发布调色作品，浏览社区内容，进行互动交流。

#### 8.4.2 性能描述

- **内容加载时间**：小于2秒
- **发布操作响应时间**：小于3秒
- **互动操作响应时间**：小于1秒

#### 8.4.3 输入

- **用户发布的作品**：前后对比照片、标题、描述、标签
- **用户互动**：点赞、评论、收藏
- **用户浏览请求**：按标签、时间等筛选条件

#### 8.4.4 输出

- **社区内容列表**：瀑布流展示的社区作品
- **作品详情**：单个作品的详细信息和互动数据
- **互动反馈**：点赞、评论、收藏的结果反馈

#### 8.4.5 程序逻辑

1. **内容发布**：用户选择前后对比照片，添加标题、描述和标签，发布到社区
2. **内容浏览**：用户浏览社区内容，可按标签、时间等筛选
3. **互动操作**：用户对作品进行点赞、评论、收藏
4. **用户关注**：用户可以关注其他用户，查看其发布的作品
5. **草稿管理**：用户可以保存未完成的作品为草稿，后续编辑和发布

#### 8.4.6 限制条件

- **发布频率**：每个用户每天最多发布10个作品
- **作品大小**：每个作品大小不超过20MB
- **评论长度**：每条评论不超过500字
- **网络连接**：需要网络连接

## 9 项目管理与实施

### 9.1 开发团队

- **队名**：哈机米南北兜队
- **成员与分工**：
  - **A**：负责智能调色与AI Agent搭建
  - **B**：负责2D转3D功能和社区搭建

### 9.2 开发流程

1. **需求分析**：分析用户需求，确定功能范围
2. **设计阶段**：设计系统架构、UI界面和数据库
3. **开发阶段**：实现前端和后端功能
4. **测试阶段**：进行功能测试、性能测试和兼容性测试
5. **部署阶段**：发布应用和部署后端服务
6. **维护阶段**：持续更新和维护系统

### 9.3 质量保证

- **代码审查**：定期进行代码审查，确保代码质量
- **测试覆盖**：编写单元测试和集成测试，提高测试覆盖率
- **性能监控**：监控系统性能，及时发现和解决性能问题
- **用户反馈**：收集用户反馈，持续改进系统

### 9.4 风险管理

- **技术风险**：AI服务不稳定、GPU渲染性能问题
- **市场风险**：用户需求变化、竞争加剧
- **运营风险**：服务器故障、数据丢失
- **法律风险**：数据隐私、知识产权

## 10 总结与展望

### 10.1 项目总结

VisionGenie项目是一款集成AI自动调色、语音控制调色、2D转3D和社区分享功能的智能照片调色应用。通过利用AI技术和语音控制，降低了照片调色的门槛，让普通用户也能轻松实现专业级的调色效果。

项目采用React Native跨平台开发框架，结合云端AI服务和本地GPU渲染技术，实现了高效、流畅的用户体验。系统架构清晰，功能模块完整，具有良好的可扩展性和可维护性。

### 10.2 未来展望

- **功能扩展**：增加更多AI功能，如智能场景识别、自动人像美化等
- **平台扩展**：支持Web端和桌面端
- **社区生态**：增强社区功能，建立创作者激励机制
- **商业化**：推出高级版和订阅服务
- **技术创新**：探索新的AI技术和交互方式，如AR调色预览等

VisionGenie项目将继续致力于为用户提供更智能、更便捷的照片调色体验，成为用户首选的照片美化工具。