# 第十九届全国大学生软件创新大赛

## VisionGenie 项目测试文档

文档编号：SWC2026-[哈机米南北兜队]  
项目名称：影像精灵 VisionGenie  
版本号：V2.0  
团队名称：哈机米南北兜队  
编写日期：2026-03-31

说明：本文根据《第十九届全国大学生软件创新大赛-项目测试文档模版》并结合当前仓库中的自动化测试、联调日志、真机截图与开发测试过程整理而成，可直接作为 Word 测试文档正文基础稿使用。

---

## 文档修订历史

| 序号 | 修订原因 | 版本号 | 作者 | 修订日期 | 备注 |
|---|---|---|---|---|---|
| 1 | 初稿编写 | V2.0 | 哈机米南北兜队 | 2026-03-31 | 基于 2026-03-31 当前项目状态整理 |

---

## 1 测试计划

### 1.1 测试策略与目标

#### 1.1.1 测试策略

本项目采用“单元测试 + 功能测试 + 系统测试 + 开发阶段回归验证”结合的测试策略，覆盖前端页面逻辑、后端模块接口、AI 调色链路、2D 转 3D 任务流程、社区交互流程以及账户认证能力。

测试过程并非在开发结束后一次性集中开展，而是伴随开发过程逐步推进，主要经历了以下阶段：

（1）后端封装与模块拆分阶段：重点验证调色、认证、建模和社区模块接口是否可以独立运行。  
（2）Tripo 接入与 2D 转 3D 调试阶段：重点观察任务创建、轮询刷新和结果返回的稳定性，并持续关注建模耗时。  
（3）Android App 联调阶段：重点验证移动端页面跳转、登录注册、调色交互、建模入口和社区展示。  
（4）功能整合与 Git 管理阶段：在模块逐步合并后，对接口契约、参数兼容性和页面状态进行回归测试。  
（5）最终测试准备阶段：集中运行自动化测试、检查日志、复测真机关键页面，并整理当前已知缺陷。

#### 1.1.2 测试目标

本阶段测试目标如下：

（1）验证核心业务链路可用，包括登录注册、图片调色、2D 转 3D、社区浏览与互动。  
（2）验证关键模块在正常输入和异常输入下均能给出合理反馈。  
（3）尽早暴露模块联调阶段存在的兼容性问题和配置问题。  
（4）评估当前版本是否具备比赛展示所需的基本稳定性。  
（5）沉淀当前已发现的问题，为后续优化和答辩准备提供依据。

综合目标不是追求“零缺陷”，而是在比赛阶段确保核心演示链路可运行、主要问题可说明、系统状态基本可控。

### 1.2 测试范围

本项目测试范围从测试广度和测试深度两个维度展开。

#### 1.2.1 测试广度

覆盖模块包括：

（1）账户与认证模块。  
（2）智能调色模块。  
（3）语音调色链路。  
（4）AI Agent 基础链路。  
（5）2D 转 3D 模块。  
（6）社区模块。  
（7）页面导航、状态管理、主题与工具模块。

#### 1.2.2 测试深度

（1）单元测试：针对函数、状态管理、接口契约和关键模块逻辑进行验证。  
（2）功能测试：针对用户在 App 中可见的业务流程进行验证。  
（3）系统测试：针对 AI/建模能力、真机联调表现和整体运行状态进行验证。  
（4）缺陷跟踪：记录最终回归阶段仍未完全修复的问题。

截至 2026 年 3 月 31 日，项目仓库内共统计到自动化测试套件 72 个，主要分布如下：

| 测试类别 | 套件数量 | 说明 |
|---|---:|---|
| backend | 21 | 后端路由、模块契约、鉴权、AI/建模配置等 |
| color | 14 | 调色引擎、自动调色、导出、分割等 |
| voice | 8 | 语音识别、语义解析、参数映射等 |
| screens | 6 | 核心页面交互与渲染 |
| agent | 5 | Agent 规划、工具注册、运行上下文 |
| 其他 | 18 | 状态管理、导航、组件、主题、API 客户端等 |

### 1.3 测试环境

#### 1.3.1 硬件环境

（1）Windows 开发主机。  
（2）Android 真机，用于页面联调、登录注册界面检查和关键功能复测。  
（3）本地网络与局域网联调环境。

#### 1.3.2 软件环境

| 环境项 | 配置 |
| --- | --- |
| Node.js | v22.14.0 |
| 前端框架 | React Native 0.84.1 |
| 后端框架 | Node.js + Express |
| 数据存储 | 前期：SQLite / better-sqlite3（过渡方案）；后期：MySQL（正式方案） |
| 包管理与脚本 | npm |
| 自动化测试工具 | Jest 29.6.3 |
| 前端测试相关 | react-test-renderer |
| 后端服务端口 | 8787 |
| 社区独立服务调试端口 | 4010 |

#### 1.3.3 网络环境

（1）本地回环环境 `127.0.0.1`。  
（2）局域网联调环境。  
（3）依赖外部 AI 与 3D 服务的联网环境。

#### 1.3.4 测试辅助工具

（1）Jest 自动化测试。  
（2）后端运行日志与错误日志。  
（3）Metro 与社区服务调试日志。  
（4）Android 真机截图与页面层级 XML。  
（5）人工回归检查。

---

## 2 单元测试

### 2.1 智能调色与语音调色模块

#### 2.1.1 测试用例与结果分析

该模块的单元测试主要覆盖调色参数计算、自动调色编排、语音指令解析、参数应用、色彩矩阵、导出契约与分割服务等核心逻辑。

单元测试用例：

| 项目 | 内容 |
|---|---|
| 用例编号 | UT-COLOR-001 |
| 测试单元描述 | 本地语音命令解析与参数映射 |
| 用例目的 | 验证用户口语化指令是否能被正确解析为调色参数修改动作 |
| 前提条件 | 已加载语音解析模块和默认调色参数 |
| 特殊规程说明 | 输入采用中文短句，如“亮一点”“色温冷一点” |
| 用例间依赖关系 | 无 |
| 具体步骤 | 1. 输入语音文本指令；2. 执行本地解析；3. 将解析结果应用到调色参数 |
| 输入 | “曝光加一点，饱和度降低” |
| 期望输出 | 返回结构化参数变更结果，且对应参数变化方向正确 |
| 实际输出 | 通过，参数映射符合预期 |
| 备注 | 对应 `voice/localParser`、`voice/paramApplier` 系列测试 |

| 项目 | 内容 |
|---|---|
| 用例编号 | UT-COLOR-002 |
| 测试单元描述 | 自动调色编排与回退逻辑 |
| 用例目的 | 验证云端不可用或超时时，系统能否进入本地降级策略 |
| 前提条件 | 自动调色 orchestrator 可运行 |
| 特殊规程说明 | 模拟超时、坏载荷与回退场景 |
| 用例间依赖关系 | 依赖基础调色参数结构 |
| 具体步骤 | 1. 调用自动调色编排；2. 注入异常返回；3. 检查回退状态 |
| 输入 | 正常图片上下文、异常云端返回 |
| 期望输出 | 返回 degraded 状态或 fallback 标记，应用不崩溃 |
| 实际输出 | 通过，降级逻辑可执行 |
| 备注 | 对应 `autoGradeOrchestrator`、`autoGradeService` 测试 |

| 项目 | 内容 |
|---|---|
| 用例编号 | UT-COLOR-003 |
| 测试单元描述 | 导出契约与颜色矩阵计算 |
| 用例目的 | 验证调色结果导出与预览参数计算一致性 |
| 前提条件 | 图像导出与颜色矩阵模块可用 |
| 特殊规程说明 | 同时检查参数边界与格式兼容性 |
| 用例间依赖关系 | 无 |
| 具体步骤 | 1. 构造调色参数；2. 生成矩阵与导出配置；3. 比较输出结构 |
| 输入 | 一组合法调色参数 |
| 期望输出 | 输出契约完整，预览矩阵和导出策略不冲突 |
| 实际输出 | 通过 |
| 备注 | 对应 `colorMatrix`、`exportSpec`、`nativeExportContract` 测试 |

测试结果分析：

该模块自动化测试覆盖度较高，是当前最稳定的部分之一。测试结果表明，本地语音解析、调色参数映射、自动调色降级策略、导出与预览契约均能正常运行。测试日志中也能看到自动调色在云端不可用时给出 degraded/fallback 状态，说明系统对异常情况已有预案。

#### 2.1.2 测试结果综合分析及建议

综合来看，智能调色与语音调色模块具备较好的单元级稳定性，适合作为比赛演示主功能。  
建议后续继续加强以下方面：

（1）增加更多中文自然语言表达方式的覆盖测试。  
（2）补充复杂场景下多条连续语音指令的叠加测试。  
（3）继续验证本地回退策略与云端结果之间的一致性表现。

#### 2.1.3 测试经验总结

本模块开发过程中最大的经验是：AI 功能不能只测试“理想输出”，必须同步测试超时、鉴权异常、服务不可达和降级场景。通过提前将回退逻辑纳入单元测试，项目在联调阶段遇到模型异常时仍能维持基本可用性。

### 2.2 后端接口与社区认证模块

#### 2.2.1 测试用例与结果分析

该部分重点验证账户认证、社区接口鉴权、后端契约、模块健康检查和数据校验逻辑。

单元测试用例：

| 项目 | 内容 |
|---|---|
| 用例编号 | UT-BACKEND-001 |
| 测试单元描述 | 账户鉴权范围校验 |
| 用例目的 | 验证不同鉴权作用域是否符合接口访问要求 |
| 前提条件 | 账户模块配置完成 |
| 特殊规程说明 | 需区分公开接口和受保护接口 |
| 用例间依赖关系 | 无 |
| 具体步骤 | 1. 构造不同作用域令牌；2. 访问目标接口；3. 校验返回结果 |
| 输入 | 有效/无效 JWT 与不同 scope |
| 期望输出 | 公开接口允许访客，写接口要求鉴权 |
| 实际输出 | 通过 |
| 备注 | 对应 `accountAuthScopes`、`authBypass` 等测试 |

| 项目 | 内容 |
|---|---|
| 用例编号 | UT-BACKEND-002 |
| 测试单元描述 | 社区数据仓库与校验器 |
| 用例目的 | 验证社区帖子、评论、互动数据的基本合法性与存取逻辑 |
| 前提条件 | 社区数据库初始化完成 |
| 特殊规程说明 | 涉及草稿、发布、评论等结构验证 |
| 用例间依赖关系 | 依赖社区数据模型 |
| 具体步骤 | 1. 构造社区数据；2. 调用校验器或仓储方法；3. 检查数据返回 |
| 输入 | 合法与非法社区数据样本 |
| 期望输出 | 合法数据通过，非法数据被拒绝 |
| 实际输出 | 通过 |
| 备注 | 对应 `communityRepository`、`communityValidators` 测试 |

| 项目 | 内容 |
|---|---|
| 用例编号 | UT-BACKEND-003 |
| 测试单元描述 | 社区认证路由初始化 |
| 用例目的 | 验证社区路由在带/不带 JWT 情况下的读写行为 |
| 前提条件 | 路由工厂可被初始化 |
| 特殊规程说明 | 需同时验证访客读取与写接口拦截 |
| 用例间依赖关系 | 依赖社区上传目录配置 |
| 具体步骤 | 1. 初始化路由；2. 发起访客/鉴权请求；3. 检查返回 |
| 输入 | 无 JWT 请求、有效 JWT 请求 |
| 期望输出 | 公开读接口可用，写接口需鉴权 |
| 实际输出 | 未完全通过，发现上传目录配置缺失问题 |
| 备注 | 对应 `communityAuth.test.js`，为当前已知缺陷之一 |

测试结果分析：

后端单元测试整体通过率较高，账户、契约、校验器和大部分路由逻辑表现稳定。但最终回归中仍发现一个社区认证相关缺陷：当社区路由初始化时，上传目录参数缺失会导致 `uploadDir` 为 `undefined`，进而引发路由初始化失败。这一问题不会否定整个社区模块的实现，但说明配置依赖在测试注入场景下仍需补强。

#### 2.2.2 测试结果综合分析及建议

当前后端模块具有较好的工程化基础，尤其在接口契约和模块拆分方面较清晰。  
建议后续重点优化：

（1）为社区模块补充默认上传目录或测试专用目录。  
（2）增强测试环境初始化脚本，避免配置缺失引发非业务型失败。  
（3）补充更多与外部服务相关的异常测试。

#### 2.2.3 测试经验总结

后端测试中的经验是：配置项和目录依赖本身也属于测试对象。许多联调问题并不是业务逻辑错误，而是环境依赖没有显式约束。通过在自动化测试中暴露这些问题，可以减少真机演示阶段的意外故障。

---

## 3 功能测试

### 3.1 用户认证与首页进入功能

#### 3.1.1 测试用例与结果分析

测试用例：

| 项目 | 内容 |
|---|---|
| 用例编号 | FT-AUTH-001 |
| 功能描述 | 用户登录注册与认证入口 |
| 用例目的 | 验证用户能否完成注册、登录并进入主界面 |
| 前提条件 | 后端认证服务正常启动 |
| 特殊规程说明 | 需同时验证登录失败提示和注册流程 |
| 用例间的依赖关系 | 依赖账户模块与 AuthGate 页面 |
| 具体步骤 | 1. 打开 App；2. 进入登录/注册页；3. 输入用户名密码；4. 提交；5. 验证跳转 |
| 输入 | 正确/错误的用户名与密码 |
| 期望结果 | 合法输入登录成功并进入主界面，非法输入给出明确提示 |
| 实际结果 | 通过，真机截图已保留登录页与注册页结果 |
| 备注 | 对应 `AuthScreen`、`AuthGate` 测试及真机截图 |

测试结果分析：

认证入口功能整体可用，自动化测试与真机截图均说明登录/注册主流程已经打通。当前该功能适合作为比赛演示中的系统入口。

#### 3.1.2 测试结果综合分析及建议

建议后续补充更完整的异常提示文案、网络错误提示和弱网登录恢复逻辑，以提升演示稳定性。

#### 3.1.3 测试经验总结

认证功能虽然属于基础链路，但它直接决定整个系统能否顺利进入主界面，因此必须优先完成真机回归。

### 3.2 智能调色功能

#### 3.2.1 测试用例与结果分析

测试用例：

| 项目 | 内容 |
|---|---|
| 用例编号 | FT-CREATE-001 |
| 功能描述 | 图片导入、AI 首轮建议与语音调色 |
| 用例目的 | 验证用户能否完成从导入图片到生成调色效果的完整操作 |
| 前提条件 | 图片选择权限正常，调色页面加载成功 |
| 特殊规程说明 | 同时检查 AI 建议、语音输入与手动参数调节 |
| 用例间的依赖关系 | 依赖调色模块、语音识别链路和预览组件 |
| 具体步骤 | 1. 选择图片；2. 进入创建页；3. 触发 AI 建议；4. 进行语音调色；5. 查看实时预览 |
| 输入 | 测试图片、语音指令 |
| 期望结果 | 页面不崩溃，可生成建议，语音指令能引起参数变化，预览可刷新 |
| 实际结果 | 通过，核心链路可演示；异常情况下会触发降级或提示 |
| 备注 | 对应 `CreateScreen.voice.test.tsx` 及调色相关测试 |

测试结果分析：

智能调色功能是当前版本表现最稳定的核心功能。测试表明，导图、AI 建议、语音调色与实时预览之间已形成完整链路，能够支撑主要展示流程。

#### 3.2.2 测试结果综合分析及建议

建议继续加强语音输入在弱网和鉴权异常下的提示质量，并补充多轮调色历史与复杂指令的交互验证。

#### 3.2.3 测试经验总结

调色功能的测试重点不是单个按钮是否可点，而是从“图片导入”到“效果反馈”整个链路是否连续。只要其中一段反馈不明显，用户体验就会显著下降。

### 3.3 2D 转 3D 功能

#### 3.3.1 测试用例与结果分析

测试用例：

| 项目 | 内容 |
|---|---|
| 用例编号 | FT-MODEL-001 |
| 功能描述 | 2D 图片建模任务创建与轮询完成 |
| 用例目的 | 验证建模任务是否可正常创建、轮询刷新并成功返回 |
| 前提条件 | 建模服务可访问，图片可上传 |
| 特殊规程说明 | 建模为异步任务，需允许较长等待时间 |
| 用例间的依赖关系 | 依赖建模后端、任务状态接口、模型展示页 |
| 具体步骤 | 1. 进入建模页；2. 上传图片；3. 创建任务；4. 轮询任务状态；5. 查看模型资源 |
| 输入 | 合法测试图片 |
| 期望结果 | 任务状态可从 queued/processing 进入 succeeded |
| 实际结果 | 通过，但任务完成耗时较长 |
| 备注 | 日志中已多次记录任务由 processing 到 succeeded 的过程 |

测试结果分析：

2D 转 3D 主流程已经具备可演示能力，日志显示任务可以被成功创建，并经过多轮状态刷新后完成。但是，该模块目前最大的现实问题仍是生成耗时较长，用户等待时间明显高于普通页面交互。

测试过程说明：项目在2D转3D功能的实现过程中，经历了多轮技术方案评估与测试。初期尝试采用腾讯混元模型，但因使用成本较高，转而使用Tripo模型，形成了初始版本的功能效果。然而，基于云端的3D模型生成方案存在处理时间较长的问题，因此团队考虑采用本地部署模型的方案，选择了SF3D模型进行测试。测试结果显示，本地部署的SF3D模型仅能生成白膜，无法实现颜色附着，且模型精度较低。经团队综合评估与讨论，最终确定采用Tripo云端模型作为最终技术方案。

#### 3.3.2 测试结果综合分析及建议

建议后续重点从两方面优化：  
一是提升等待过程中的状态可视化和用户提示；  
二是继续分析第三方模型服务的调用效率，减少无效轮询与长时间停留在 processing 状态的体验损耗。

#### 3.3.3 测试经验总结

建模功能测试不能只看最终是否成功，更要关注等待过程是否“可解释”。对于耗时较长的能力，良好的状态提示本身就是用户体验的一部分。

### 3.4 社区浏览与互动功能

#### 3.4.1 测试用例与结果分析

测试用例：

| 项目 | 内容 |
|---|---|
| 用例编号 | FT-COMMUNITY-001 |
| 功能描述 | 社区动态浏览、点赞、收藏与评论 |
| 用例目的 | 验证社区基础互动流程能否运行 |
| 前提条件 | 社区接口服务已启动，测试账号可用 |
| 特殊规程说明 | 需分别验证公开浏览和鉴权后操作 |
| 用例间的依赖关系 | 依赖登录、社区接口与个人信息 |
| 具体步骤 | 1. 进入社区页；2. 浏览动态；3. 进入详情；4. 执行点赞/收藏/评论 |
| 输入 | 测试帖子与评论内容 |
| 期望结果 | 动态可展示，互动结果回写成功 |
| 实际结果 | 基础流程通过，社区模块具备展示能力 |
| 备注 | 自动化测试覆盖社区页面、仓储和校验逻辑 |

测试结果分析：

社区功能当前已具备基本的展示与互动链路，适合在比赛中作为扩展模块进行演示。需要注意的是，独立社区服务日志中曾出现依赖安装缺失和外键约束异常，说明该模块在独立运行时仍需进一步规范初始化过程。

#### 3.4.2 测试结果综合分析及建议

建议统一社区模块的初始化脚本和测试数据准备方式，避免因环境差异影响评论、上传或数据写入。

#### 3.4.3 测试经验总结

社区模块的难点不在界面展示，而在于它同时依赖账号、数据、上传和鉴权。测试时必须把“环境初始化”作为完整流程的一部分处理。

---

## 4 系统测试

### 4.1 模型性能测试

#### 4.1.1 测试用例与结果分析

本节系统测试主要关注两个高耗时或高依赖场景：AI 首轮分析/语音链路，以及 2D 转 3D 模型生成流程。

预期性能指标测试用例：

| 项目 | 内容 |
|---|---|
| 用例编号 | ST-PERF-001 |
| 性能描述 | AI 首轮视觉建议返回时间 |
| 用例目的 | 评估联网条件下 AI 首轮建议的可用性与耗时 |
| 前提条件 | 后端服务启动，模型接口配置完成 |
| 特殊规程说明 | 同时记录异常时的降级情况 |
| 用例间的依赖关系 | 依赖图像理解与调色建议服务 |
| 具体步骤 | 1. 上传图片；2. 触发首轮建议；3. 记录日志中的返回耗时 |
| 输入/动作 | 调用 initial visual suggest |
| 期望的性能（平均值） | 30 秒内返回结果或给出降级反馈 |
| 实际的性能（平均值） | 真实模型样本日志记录为 24766 ms；异常情况下可在 1 秒内降级返回 |
| 备注 | 数据来自 `backend-runtime.log` |

| 项目 | 内容 |
|---|---|
| 用例编号 | ST-PERF-002 |
| 性能描述 | 2D 转 3D 异步任务完成过程 |
| 用例目的 | 评估建模任务是否能在可接受的轮询周期内完成 |
| 前提条件 | 建模服务正常、第三方服务可用 |
| 特殊规程说明 | 重点观察 processing 阶段轮询次数 |
| 用例间的依赖关系 | 依赖 image-to-3d 服务 |
| 具体步骤 | 1. 创建建模任务；2. 轮询任务状态；3. 记录从创建到成功的过程 |
| 输入/动作 | 提交单张图片建模 |
| 期望的性能（平均值） | 任务可稳定完成，并能提供状态反馈 |
| 实际的性能（平均值） | 多次测试均可完成，但通常需要多轮轮询后才进入 succeeded |
| 备注 | 数据来自 `backend-start.log` 与 `backend-realdevice.log` |

| 项目 | 内容 |
|---|---|
| 用例编号 | ST-PERF-003 |
| 性能描述 | 自动化回归测试通过率 |
| 用例目的 | 评估当前版本整体工程稳定性 |
| 前提条件 | 依赖已安装，Jest 可运行 |
| 特殊规程说明 | 以 2026-03-31 当前仓库状态为准 |
| 用例间的依赖关系 | 依赖全部自动化测试套件 |
| 具体步骤 | 1. 在项目根目录运行 `npm test`；2. 汇总通过与失败情况 |
| 输入/动作 | 执行 Jest 回归测试 |
| 期望的性能（平均值） | 大部分测试通过，失败项可定位 |
| 实际的性能（平均值） | 72 个测试套件中 70 个通过；218 个测试点中 214 个通过 |
| 备注 | 仍有 2 个失败套件待修复 |

测试结果分析：

系统测试结果表明，VisionGenie 当前版本已经具备较完整的功能基础和较高的工程稳定性。自动化回归测试整体通过率较高，核心链路如调色、页面渲染、账户认证、建模接口和社区数据逻辑均已具备较强支撑。

同时，系统测试也暴露出当前版本的几个典型问题：

（1）AI 相关能力对外部模型服务依赖较强，存在鉴权、超时和网络波动影响。  
（2）2D 转 3D 生成链路耗时较长，是当前用户等待感最强的部分。  
（3）最终自动化回归仍存在 2 类待修复问题：

1. `queryClient.test.ts` 中暴露出 queryClient 默认配置接口调用方式不匹配。  
2. `communityAuth.test.js` 中暴露出社区上传目录配置缺失，导致路由初始化失败。

#### 4.1.2 测试结果综合分析及建议

从系统层面看，当前版本已经达到“核心能力可演示、主要问题可定位”的阶段，适合作为比赛版本继续打磨。后续建议如下：

（1）优先修复自动化回归中剩余的 2 个失败套件，提升最终回归完整性。  
（2）继续优化 2D 转 3D 流程的等待体验和状态提示。  
（3）进一步完善 AI 链路的鉴权检查、超时处理和降级提示。  
（4）在正式提交前再次执行一轮真机回归，重点复测登录、调色、建模、社区与截图展示页面。

#### 4.1.3 测试经验总结

本项目的系统测试经验主要体现在三点：

（1）AI 与外部模型服务相关功能必须同时设计“正常路径”和“异常路径”的测试方案。  
（2）对于耗时较长的功能，性能测试不应只看完成时间，更要关注用户是否知道系统正在做什么。  
（3）最终阶段的测试价值不仅在于证明系统“能跑”，更在于提前暴露那些会影响比赛现场演示的问题。

---

## 当前测试结论

截至 2026 年 3 月 31 日，VisionGenie 项目已经完成了覆盖单元测试、功能测试和系统测试的阶段性验证。测试结果显示：

（1）智能调色、语音调色、账户认证、页面展示和大部分后端契约逻辑表现稳定。  
（2）2D 转 3D 模块已经具备可运行能力，但生成耗时仍然偏长。  
（3）社区模块基本能力已形成，但配置初始化和独立运行环境仍需继续规范。  
（4）自动化回归整体通过率较高，当前剩余问题已具备明确定位方向。

因此，可以认为项目当前已具备比赛展示所需的核心测试基础，下一步重点在于缺陷收敛、真机回归强化和展示链路打磨。
